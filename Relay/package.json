{
  "name": "rook-mobile-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Opaque WebSocket relay for paired Rook Mac and iPhone clients",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "dev": "wrangler dev --local",
    "test": "node --test test/relay.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260812.1",
    "wrangler": "^4.0.0",
    "ws": "^8.18.0"
  }
}
